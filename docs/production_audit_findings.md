# Production-readiness audit — confirmed findings (2026-07-04)

Whole-system audit with real artifacts. 33 confirmed, 4 dismissed of 37 raised.
Theme: integration seams from parallel builds + one safety regression. Most fail CLOSED (block deploy) not open.

## [CRITICAL] No post-show outcome capture in the rebuilt UI — a real-robot incident can never be recorded, so a dance that fell stays show-ready and gets redeployed
- **Where:** `ui/static/app.js:316-321 (runChecklist deploy handler), app.js:279+284 (outcome display-only); ui/server.py:552-562 (record_outcome endpoint); pipeline/shows.py:477-506 (demote-on-incident)` (integration / deploy-safety regression)
- **Why real:** Verified in the real code. grep of ui/static/app.js finds `outcome` only twice — app.js:279 (history table cell) and app.js:284 (outcomeBadge helper) — both pure display; there is NO POST to /api/shows/{id}/outcome anywhere in the client. The deploy handler (app.js:316-321) terminates with `toast(...); bg.remove(); RENDER.show();` and offers no clean/aborted/incident follow-up step. The server endpoint record_outcome (server.py:552) and shows.rec
- **Fix:** Add a post-deploy outcome step to the checklist wizard: after the deploy record succeeds (app.js:319), instead of closing the modal, render Clean / Aborted / Incident buttons that POST to /api/shows/{sh.id}/outcome. Give Aborted/Incident btn-danger styling plus a confirm() warning ('marks the dance not-ready, requires re-verification'). Until a show is closed with an outcome, surface it in RENDER.

## [HIGH] Signed verdict binds motion by the .npz sha, but every laptop consumer binds by the .csv sha — the real Thriller verdict can never authorize show-ready or a deploy bundle
- **Where:** `pipeline/mjlab_verify.py:52 / pipeline/shows.py:262,265-266 / deploy/gen_config.py:75 / pipeline/exam_verdict.py:131-132` (integration/cross-module seam)
- **Why real:** Verified on disk and in code. The mjlab held-out eval runs on the .npz motion (data/policies/thriller/heldout_eval.json motion_file=.../thriller_show.npz), and mjlab_verify.py:52 signs motion_sha256=full_sha256(--motion). The committed real verdict (heldout_verdict.json) therefore carries motion_sha256=10883a611e7d... (the npz digest). Every laptop consumer instead hashes the deployable CSV: shows.record_sim_run_from_verdict hashes dance.motion_c
- **Fix:** Make the verdict's motion identity the DEPLOYABLE motion the dance and bundle actually carry (the CSV), not the mjlab .npz that was evaluated. Either (a) invoke mjlab_verify --motion with data/motions/thriller/thriller_show.csv so motion_sha256 becomes the CSV digest (accepting that it no longer equals the byte-identity of the evaluated npz — document the provenance link), or preferably (b) record

## [HIGH] deploy/gen_config.py cannot consume a passing mjlab_heldout verdict: exam_*.json glob never matches heldout_verdict.json AND nominal.duration_s KeyError at manifest build
- **Where:** `deploy/gen_config.py:47,138` (integration/deploy-path-broken)
- **Why real:** Verified both defects in the real code. (1) gen_config.py:47 globs `exam_*.json`; the real/only passing-verdict producer, mjlab_verify.py, writes an operator-named --out (real file: data/policies/thriller/heldout_verdict.json), which does not start with `exam_` and is not in the default --exam-dir (data/exports/, empty). No converter renames it (grep of pipeline/, cloud/, ui/, app/ found none). (2) gen_config.py:138 reads verdict['nominal']['dura
- **Fix:** Make gen_config consume the mjlab verdict: broaden find_passing_exam to match by schema=='sim_exam/v1' over '*.json' (or add an explicit --verdict path) instead of the exam_*.json filename glob, and point default discovery at the policy dir. For duration_s, either derive motion duration from the motion CSV itself (len(rows)/CSV_FPS) rather than reading verdict['nominal']['duration_s'], or populate

## [HIGH] MEC footprint gate certifies a radius the deployed motion never respects (excursion-from-start can be ~2x the certified footprint → robot leaves the dance area)
- **Where:** `pipeline/vet_motion.py:75-88 (+ pipeline/stages/local_motion.py:99-105 vs pipeline/find_window.py:112-114)` (safety-gate-soundness)
- **Why real:** Verified real and reachable in the production code path, with a concrete demonstration on the shipped artifact. vet_motion.py:81-88 certifies the translation-invariant MEC footprint radius and its own comment (79-80) states the guarantee only holds if the robot is placed at footprint_center_xy. The production retarget stage local_motion.py:103 exports the window recentered on FRAME 0 (`seg[:,0:2] -= seg[0,0:2]`), and vet runs on that frame-0-rece
- **Fix:** Make the exported/deployed motion consistent with the metric vet certifies: in local_motion.py replace the frame-0 recenter (line 103) with a recenter on the window's enclosing-circle centre — call find_window.window_center(seg, 0, len(seg)-1) and subtract it, exactly as find_window.main() already does — so the deployed start equals the certified footprint centre and excursion-from-start = radius.

## [HIGH] 02_push_bundle.sh integrity re-check is dead code: reads a manifest key gen_config no longer writes (KeyError) and compares truncated-16 vs full-64 sha
- **Where:** `deploy/02_push_bundle.sh:27-38` (deploy-safety)
- **Why real:** Verified against real code, not missed by the reviewer. gen_config.py:141 writes the manifest exam sub-object as {"file":"exam_verdict.json","authorized":True} with NO "verdict" key, so 02_push_bundle.sh:32 `assert man["exam"]["verdict"]=="pass"` raises KeyError: 'verdict' (reproduced directly). The heredoc runs at lines 27-38, before the DRY_RUN branch (line 43) and gated only by the bundle.json existence check (line 24), so ANY real gen_config 
- **Fix:** Rewrite the heredoc to the actual deploy_bundle/v1 schema: assert man["exam"]["authorized"] is True (not ["verdict"]), and hash-verify every file against man["files_sha256"] using the FULL hexdigest (drop [:16]). Stronger: import pipeline.exam_verdict.authorize/full_sha256 and re-derive authorization from the bundled exam_verdict.json against the recomputed policy/motion shas, so the guard re-veri

## [HIGH] gen_config.py:138 KeyError 'duration_s' — a passing mjlab verdict authorizes then crashes bundle generation (verdict-producer schema drift)
- **Where:** `deploy/gen_config.py:138` (integration/schema-drift)
- **Why real:** Verified by execution. gen_config.py:138 reads verdict["nominal"]["duration_s"], but pipeline/mjlab_verify.py build_verdict (lines 54-62) — the LIVE verification producer, the one that generated the only real artifact data/policies/thriller/heldout_verdict.json — emits a nominal dict WITHOUT duration_s (keys: pass, success_rate, n_success, num_episodes, mpkpe_m, ee_pos_error_m, held_out_seed). Only pipeline/sim_exam.py:655 emits duration_s. Both 
- **Fix:** Make the manifest tolerant of both sim_exam/v1 producers: duration_s = verdict["nominal"].get("duration_s"), and if absent derive it from the motion CSV row count / fps (the bundle already copies motion.csv). Better: unify the sim_exam/v1 nominal schema so sim_exam.py and mjlab_verify.py emit identical required keys, and add a regression test that round-trips a real signed mjlab verdict through ge

## [HIGH] Library import trusts dance.json wholesale — injects a fake 'show-ready' dance, bypassing the signed-verdict gate the API enforces
- **Where:** `pipeline/library.py:108-134` (security/trust-model-bypass)
- **Why real:** Verified end-to-end in the real code; the reviewer did not miss a guard. import_library() (pipeline/library.py:108-134) rebuilds each dance by writing the archive's dance.json verbatim (line 134); the loop only rewrites the three file-path fields in _FILE_FIELDS (motion_csv, policy_path, preview) and never resets status, sim_exam, policy_sha256, or repeatability.consecutive_clean. The only import-time guards are the schema-string check (106), pat
- **Fix:** On import, do not trust status/sim_exam/policy_sha256/repeatability from dance.json. Reset each imported dance to status='draft', clear sim_exam and policy_sha256, and zero consecutive_clean (mirroring shows.attach_policy at shows.py:349-370), forcing a fresh local signed sim-exam before it can be promoted. If verified state must survive a legitimate backup round-trip, re-authenticate on import by

## [HIGH] No UI control to promote a sim-verified dance to show-ready — the operator cannot make any dance show-ready through the app
- **Where:** `ui/server.py:452-458 (promote route); ui/static/app.js:183 (modal, no promote button); ui/static/app.js:151 (nextSteps misdirect); pipeline/shows.py:318-346 (promote is sole path to show-ready)` (workflow-gap)
- **Why real:** Verified against the real code, all claims hold. (1) The promote route exists at ui/server.py:452-458; grepping the whole frontend (app.js + index.html) for "promote" returns nothing — no caller. (2) promote() in pipeline/shows.py:318-346 is genuinely the ONLY path to status show-ready: _apply_sim_run (behind the verdict endpoint) advances only draft->sim-verified (shows.py:296-297) and never sets show-ready; the only status="show-ready" assignme
- **Fix:** Add a "Promote to Show-Ready" button in the dance-detail modal (openDance, app.js:178-184) shown when d.status === "sim-verified", POSTing to /api/dances/{id}/promote with {status:"show-ready"} and surfacing the server's ValueError text (e.g. "2/3 consecutive clean sim runs" or "policy file changed since the passing exam") as an inline error, then refreshDances(). Repoint the Dashboard Next Steps 

## [MEDIUM] Configurable venue max_excursion is orphaned — never reaches windowing, vet, mjlab verdict, or heldout eval (always 1.5 m)
- **Where:** `pipeline/stages/local_motion.py:99,115 / ui/server.py:341-357 / pipeline/mjlab_verify.py:40,96 / cloud/heldout_eval.py:86-127` (integration / orphaned-feature / false-capability)
- **Why real:** Every cited line verifies. The per-venue spatial gate (venue.py Venue.max_excursion_m, honored by find_window.longest_window and vet_motion via --max-excursion / G1_MAX_EXCURSION_M) is genuinely never threaded through the real pipeline: (1) local_motion.py:99 calls longest_window(m) with the constant 1.5 default — this in-process call does not even read the env var; (2) local_motion.py:115 runs vet with no env_extra and server.py:353 runs vet wit
- **Fix:** Either (a) thread the selected venue end-to-end — pass venue.max_excursion_m into longest_window at local_motion.py:99, set G1_MAX_EXCURSION_M in the vet subprocess env at local_motion.py:115 and server.py:353, pass --max-excursion-m to mjlab_verify, enforce a per-episode root-excursion cap in heldout_eval.py, and add a venue selection endpoint so a non-default venue is reachable; or (b) explicitl

## [MEDIUM] Venue parameterization is entirely unwired — spatial gate is permanently the 1.5 m default regardless of venue
- **Where:** `pipeline/config.py:21-23, pipeline/stages/local_motion.py:99 & 115` (integration/dead-feature (false config contract))
- **Why real:** Verified every specific claim against the real code, default-skeptical, and the reviewer did NOT miss a guard. local_motion.py:99 calls longest_window(m) with no max_excursion_m arg (defaults to 1.5 m, find_window.py:43). local_motion.py:115 calls _run_tool("vet_motion.py", [str(motion_csv), "--json"], job) with NO env_extra, so the subprocess inherits an env where G1_MAX_EXCURSION_M is unset and vet_motion.py:32 falls back to float(os.environ.ge
- **Fix:** Either (a) wire venue through: add venue selection to the job payload/server, and in RetargetStage.run pass max_excursion_m=venue.max_excursion_m to longest_window and env_extra={config.VENUE_EXCURSION_ENV: str(venue.max_excursion_m)} to the vet_motion.py subprocess, and make shows.py venue_ack render the chosen radius; or (b) if venue flexibility is out of scope for first paid shows, delete the f

## [MEDIUM] dedupe_dances back-fills motion_csv path from an imported loser, then rmtree's the loser's directory — dangling motion reference (policy/preview NOT actually reachable)
- **Where:** `pipeline/shows.py:224-237 (with library.py:118-134, 33)` (integration/data-integrity)
- **Why real:** The core mechanism is real and unguarded. dedupe_dances (run every startup via seed_initial_dances, shows.py:522) back-fills file-valued fields from a loser into the keeper as raw path strings (shows.py:225-228), then unconditionally `shutil.rmtree(loser.dir)` (shows.py:236) with no check that the keeper now references a file inside that dir. import_library (library.py:118-134) rewrites an imported record's file fields to paths INSIDE data/dances
- **Fix:** In dedupe_dances, before rmtree, treat file-valued fields specially: for any field back-filled (or already held) whose resolved path lives under loser.dir, copy the underlying file into the keeper's own dir (or a stable data/ location) and rewrite the keeper's path before deleting; equivalently, skip rmtree of a loser whose dir still contains a file the keeper references after merge. In practice o

## [MEDIUM] Desktop launch silently attaches to a stale server already holding port 8735 (stale-process/port issue unmitigated)
- **Where:** `ui/desktop.py:19-48` (reliability / operational-safety)
- **Why real:** Verified against real code. ui/desktop.py:42 launches uvicorn.Server.run() in a daemon thread; if 127.0.0.1:8735 is already bound (crash leftover, or a foreground `python ui/server.py`), uvicorn logs the bind error and the thread dies — SystemExit inside a daemon thread does not reach main(), which keeps running. _wait_for_port (lines 19-27) uses a bare socket.create_connection with no HTTP call and no identity check, so it immediately succeeds a
- **Fix:** Before launching, ensure the port is free or owned by us: write logs/app.pid on start and, if the port is bound, verify/kill the recorded PID or abort loudly. Add a per-process boot nonce to /api/health and make _wait_for_port poll GET /api/health and require the nonce it just generated, so it only accepts the server this process spawned instead of any raw TCP listener. Also surface the daemon-thr

## [MEDIUM] cloud.json written non-atomically and read unguarded — a truncated config 500s the System/Cloud panels instead of degrading
- **Where:** `pipeline/cloud.py:46-49` (reliability/graceful-degradation)
- **Why real:** All claims verified against real code. cloud.py:46-49 save_config() uses bare CONFIG_PATH.write_text(json.dumps(...)) with no tmp+os.replace+fsync — the only store skipping the durable-write pattern the codebase mandates elsewhere (store.py:38-44 _durable_write, venue.py:166-182, shows.py:79-83, all documented as an audit-required durability fix). load_config() (cloud.py:40-43) only guards the missing-file case; a corrupt/truncated file goes stra
- **Fix:** Make save_config write via tmp file + f.flush() + os.fsync() + os.replace (mirror _durable_write in store.py/venue.py/shows.py, preserving the 0o600 chmod on the tmp before replace), and wrap load_config's json.loads in a try/except json.JSONDecodeError that logs to stderr and falls back to DEFAULT_CONFIG so a corrupt config degrades gracefully instead of 500ing both panels.

## [MEDIUM] Unvalidated dance id from archive manifest used as filesystem path in library import (partial path traversal / arbitrary write under data/)
- **Where:** `pipeline/library.py:108-117,139-146` (path-traversal)
- **Why real:** Verified against the real code. import_library iterates manifest['dances'] (line 108) and uses each did unvalidated in both src_dir = root/'dances'/did (109) and dest_dir = shows.DANCES_DIR/did (114, DANCES_DIR = DATA_DIR/'dances' per shows.py:37), then mkdir(parents=True) (117) and copyfile (126,134). _safe_extract (139-146) only validates tar MEMBER names, not the manifest dances list, and did is read from manifest.json content. Scenario is gen
- **Fix:** Before using did in any path (lines 109/114), validate it against a strict pattern such as ^[A-Za-z0-9_-]{1,64}$ (reject '.', '..', separators), and assert (DANCES_DIR/did).resolve().is_relative_to(DANCES_DIR.resolve()); skip/reject the manifest entry otherwise. Note the fix's DANCE_RE/gen_config.py reference is stale — that symbol/file does not exist here, so introduce the regex locally.

## [MEDIUM] Deploy-safety regression: 02_push_bundle.sh integrity re-check is broken against the current bundle manifest schema
- **Where:** `deploy/02_push_bundle.sh:32,35-36` (deploy-safety)
- **Why real:** Verified against the real files. gen_config.py's manifest (line 141) writes `"exam": {"file": "exam_verdict.json", "authorized": True}` — there is NO "verdict" key, so 02_push_bundle.sh line 32 `assert man["exam"]["verdict"] == "pass"` raises KeyError (not a failed assertion) on every real bundle. Independently, lines 35-36 compute `hashlib.sha256(...).hexdigest()[:16]` (16 hex chars) and compare against `man[part]["sha256"]`, which is `p_sha`/`m
- **Fix:** Rewrite the heredoc to (a) drop the `[:16]` and compare the full sha256 of every bundle file against `man["files_sha256"][name]` (which is what gen_config.py actually pins, lines 125-129/142), and (b) authenticate the exam by loading exam_verdict.json from the bundle and calling `pipeline.exam_verdict.authorize(verdict, policy_sha=..., motion_sha=...)` — never read a manifest string. Do not fix th

## [MEDIUM] SSH transport disables host-key verification for an internet-reachable GPU box, enabling MITM credential/command capture
- **Where:** `pipeline/cloud.py:28-34,92-108` (security)
- **Why real:** Confirmed in the real code, not a reviewer hallucination. SSH_BASE_OPTS (lines 28-34) literally sets `StrictHostKeyChecking=no` AND `UserKnownHostsFile=/dev/null` — the maximal-exposure combination: not only is an unknown key auto-accepted, no key is ever persisted or compared across sessions, so even a key change mid-session against a pinned expectation is impossible to detect. These opts are applied to every `run()` call (line 205-207 -> `_run_
- **Fix:** Replace `StrictHostKeyChecking=no` + `UserKnownHostsFile=/dev/null` with `StrictHostKeyChecking=accept-new` and a real known_hosts file under `.secrets/` (e.g. `.secrets/known_hosts`, chmod 600) so the box's key is TOFU-pinned on first connect and verified thereafter; on operator-initiated instance restart (which regenerates the key), have the UI prompt to re-pin explicitly rather than silently ac

## [MEDIUM] No in-app way to run the sim-exam or submit its verdict — the app instructs a step it cannot perform
- **Where:** `pipeline/stages/local_motion.py:163-167 (VerifyStage stub); pipeline/sim_exam.py:764-868 (CLI only, writes JSON, does not POST); ui/server.py:461-483 (accepts verdict, does not launch exam); ui/static/app.js:189-194 (attach toast, no verify button)` (workflow-completeness / UX)
- **Why real:** Verified reachable and unguarded. The in-app pipeline's VerifyStage.run() is a hard stub (raises StageBlocked at local_motion.py:167), so the Studio job flow never runs the sim-exam. sim_exam.py is CLI-only; it writes a signed verdict to data/exports/ and does NOT POST it (the finding's claim that the CLI POSTs the verdict is inaccurate — making the gap worse, requiring a second manual submit). ui/server.py:461 only accepts a verdict; no route la
- **Fix:** Add a 'Run sim-exam' action on any policy-bearing dance in the dance-detail modal that launches pipeline/sim_exam.py as a background subprocess (reuse the server.py:352 pattern) against the dance's policy_path + motion_csv, and on completion self-submits the signed verdict JSON to /api/dances/{id}/sim-runs, surfacing progress and the pass/fail result. Minimum viable fix: replace the app.js:192 toa

## [MEDIUM] Music/soundtrack never reaches the operator flow — audio-sync module is orphaned (built but wired to nothing)
- **Where:** `pipeline/audio.py:1-252 (orphaned); pipeline/shows.py:112-134 (Dance has no music field); pipeline/stages/local_motion.py:139-150 (preview rendered silent, never muxed); ui/server.py + ui/static/app.js (no audio anywhere)` (integration-gap / orphaned-code / missing-feature)
- **Why real:** The factual core of the finding is fully confirmed against the real tree (not worktrees):

1. pipeline/audio.py is genuinely orphaned. grep for imports shows it is imported ONLY by tests/test_audio.py; nothing in pipeline/ or ui/ imports it. Its three jobs (ingest/align/mux) and its careful prep-pad delay (audio_delay_s = PAD_IN_S + BLEND_IN_S = 1.5s, audio.py:103) are never invoked by any operator path. It even produced a one-off data/previews/t
- **Fix:** Persist aligned audio per dance (data/dances/<id>/audio.*) at ingest, add music_file + audio_delay_s to the Dance record, mux it onto the rendered preview (pipeline/stages/local_motion.py) so library previews play with music, and give Show mode a single synchronized start cue that begins music and the deploy record together using audio.compute_alignment's delay. Alternatively, if music is intentio

## [MEDIUM] Flexible-venue model is backend-only — no UI/API wiring; retarget + checklist hardcode the 1.5 m/2 m default, overflow choice unreachable
- **Where:** `ui/server.py (no venue route, lines 132-565); pipeline/stages/local_motion.py:99 and :115; pipeline/venue.py:229 (fit_motion_to_venue, no callers); pipeline/shows.py:60,69-72` (integration-gap / dead-feature)
- **Why real:** Verified against real code. (1) ui/server.py's complete route table has no /api/venue endpoint. (2) local_motion.py:99 calls longest_window(m) with no max_excursion arg (defaults to MAX_EXCURSION_M=1.5 in find_window.py:43); and the vet subprocess at local_motion.py:115 is run with no env_extra, so vet_motion.py:32's G1_MAX_EXCURSION_M lookup also falls back to 1.5 — the whole retarget window+gate path is pinned to 1.5 m. (3) shows.py checklist h
- **Fix:** Add a server route to list/create venues (radius/width-depth + margin) backed by the existing venue.py persistence. Thread the selected venue's max_excursion_m into the retarget path: pass it to longest_window(m, max_excursion_m=...) at local_motion.py:99 AND set env_extra={"G1_MAX_EXCURSION_M": str(maxexc)} on the vet_motion subprocess so the gate and windowing agree. On overflow, surface fit_mot

## [MEDIUM] App nags the operator to delete the GPU box to stop billing but offers no in-app stop/delete control or cap-crossing alert, while spend accrues in real time
- **Where:** `ui/static/app.js:135, ui/static/app.js:351 (billing hints); ui/server.py:270-287 (cloud routes are config/test only); pipeline/monitor.py:109 (over_cap computed but unused)` (ux-safety / cost-control)
- **Why real:** Verified against real code. app.js:135 (Dashboard Budget card) and app.js:351 (System panel) both tell the operator 'Billing runs until the box is deleted — not just stopped,' and the cap is rendered as a gauge/bar (app.js:133,349-350,95). But the server exposes only cloud config-save and connection-test routes (server.py:275,283); a full grep finds NO stop/delete/terminate/lifecycle route or button anywhere, and no cap-crossing banner/toast is e
- **Fix:** Wire the already-computed monitor.py over_cap (and a lower ~80% cap_fraction threshold) to the existing banner/toast so a persistent alert fires when spend nears/exceeds the cap, with a direct deep link to the GreenNode console and the exact delete steps. Add a guarded 'I've deleted the box' acknowledgement that stops the local cost-accrual clock. Soften app.js:135 to reflect the team's own UNVERI

## [LOW] Deploy docs advertise a sim_exam CLI command that KeyErrors on the real policy_meta.json (schema drift), but the actual gate is intact
- **Where:** `pipeline/sim_exam.py:264 / data/policies/thriller/policy_meta.json / deploy/gen_config.py:81 / deploy/README.md:24-25` (documentation-drift)
- **Why real:** Verified against real code and artifacts. The schema drift is real: sim_exam.py:264 does meta["joint_names"] unconditionally and load_policy (line 357) routes any onnx with an adjacent policy_meta.json to MjlabOnnxPolicy. The real data/policies/thriller/policy_meta.json carries actor_obs_terms_in_order, a regex-dict action_scale, tracked_body_names, onnx_inputs — none of joint_names/kp/kd/default_joint_pos/obs_terms. So the command literally prin
- **Fix:** Point the docs at the live flow instead of the retired one: update deploy/gen_config.py:81's ABORT hint and deploy/README.md:24-25 to say "run python -m pipeline.mjlab_verify (emits the signed sim_exam/v1 verdict)". Optionally either retire the MjlabOnnxPolicy CLI path or make its __init__ read the real mjlab schema (actor_obs_terms_in_order, regex-dict action_scale expanded against joint names, k

## [LOW] policy_meta.json per-joint action_scale (regex dict) has no executing in-repo consumer; sim_exam's dict-mishandling at 269-270 is unreachable
- **Where:** `pipeline/sim_exam.py:264-270 / data/policies/thriller/policy_meta.json` (test-coverage / integration-verification gap)
- **Why real:** The finding's substantive claim is TRUE and grounded in the real artifacts. data/policies/thriller/policy_meta.json ships action_scale as a 16-entry regex->value DICT (0.0745 for .*_wrist_pitch/yaw_joint), and no executing in-repo code applies these per-joint values: (1) cloud/heldout_eval.py never opens policy_meta.json — it does env.step(policy(obs)) and lets the mjlab env cfg supply the scale; (2) grep shows NO .py reads actor_obs_terms_in_ord
- **Fix:** Reconcile the two diverged schemas first: either update MjlabOnnxPolicy to read the actually-emitted policy_meta.json (regex-dict action_scale expanded per joint via actor_obs_terms_in_order/joint order, plus the 160-dim anchor+IMU obs adapter the in_engine_eval caveat calls out) so sim_exam can load the shipped artifact, or bring the deploy-side action mapping in-repo. Then add one executable tes

## [LOW] Two different show-ready survival bars (100% sim_exam vs 99% mjlab) flow through the same authorize() gate
- **Where:** `pipeline/sim_exam.py:728` (correctness/consistency)
- **Why real:** Verified in the real code and reachable; no guard was missed. exam_verdict.derive_pass (the single deploy gate, invoked by gen_config.find_passing_exam at deploy/gen_config.py:52) requires _phase_passed(repeat) at exam_verdict.py:103, which trusts the harness-set repeat['pass'] is True (line 90), AND independently recomputes clean>=3 and clean/runs>=0.99 at line 110. sim_exam.run_repeatability sets repeat['pass']=clean==runs (sim_exam.py:728 → 10
- **Fix:** Compute repeat['pass'] in sim_exam.run_repeatability using the shared REQUIRED_CLEAN_RUNS/REQUIRED_CLEAN_RATE imported from exam_verdict (as mjlab_verify already does), so both harnesses feed one consistent bar into derive_pass; keep derive_pass as the single source of truth. This makes the user's deliberate 99% show-ready decision uniform across both signing paths.

## [LOW] mjlab_verify docstring/comments describe the wrong (stricter) safety bar than the code enforces
- **Where:** `pipeline/mjlab_verify.py:12-14,27` (documentation-accuracy)
- **Why real:** Verified against the real code. pipeline/mjlab_verify.py:78 enforces repeatability via (clean / n) >= REQUIRED_CLEAN_RATE, and pipeline/exam_verdict.py:30 sets REQUIRED_CLEAN_RATE = 0.99 (the comment at exam_verdict.py:27-28 explicitly notes it "Was a strict 100% (clean == runs); loosened to 0.99 deliberately"). Yet the mjlab_verify module docstring (lines 12-14) still states "repeatability is clean==runs (i.e. EVERY held-out episode survived)" a
- **Fix:** Update the stale docstring (lines 12-14) and inline comment (line 27) in pipeline/mjlab_verify.py to state the real bar: repeatability passes at survival rate >= REQUIRED_CLEAN_RATE (0.99, the >=99% standard) across >= REQUIRED_CLEAN_RUNS episodes, matching exam_verdict.py and the enforced code at line 78 and the already-correct comment at lines 74-76.

## [LOW] fit_motion_to_venue runs the window's absolute-z (pelvis>=0.35) test on an un-grounded motion
- **Where:** `pipeline/venue.py:229-265` (correctness/consistency (latent))
- **Why real:** Verified real, and the reviewer did not miss a guard. venue.py:265 passes the raw `motion` straight into `longest_window`, which applies an absolute-z floorwork gate at find_window.py:56 (pelvis z >= 0.35 with floor at z=0); find_window.py:49-50 explicitly requires a GROUNDED motion. `fit_motion_to_venue` never grounds, so its docstring claim (venue.py:235-236) that the result is grounding-agnostic ("grounded or not — XY is unaffected by z") is o
- **Fix:** Ground the motion at the top of fit_motion_to_venue before computing the report, mirroring find_window.main(): guard with pipeline.grounding.have_model() and call ground_motion() when available, then drop/correct the "grounded or not" clause in the docstring. This keeps the suggested_window's absolute-z gate meaningful if/when the venue report is wired into the UI. Not deploy-blocking; recommend f

## [LOW] mjlab_verify.py docstring asserts a strict 100% (clean==runs) show-ready bar the code no longer enforces (99%)
- **Where:** `pipeline/mjlab_verify.py:12-14,27` (documentation)
- **Why real:** The factual core of the finding is CONFIRMED. mjlab_verify.py:12-14 states show-ready "requires ... repeatability is clean==runs (i.e. EVERY held-out episode survived)" and line 27 says "show-ready still needs repeatability clean==runs (100%) via derive_pass." The actual gate does NOT enforce clean==runs. exam_verdict.py:30 sets REQUIRED_CLEAN_RATE=0.99, and both derive_pass (line 110: `clean >= REQUIRED_CLEAN_RUNS and (clean / runs) >= REQUIRED_
- **Fix:** Rewrite the mjlab_verify.py module docstring (lines 12-14) and the line-27 comment to state the real bar: show-ready needs derive_pass = all phases pass + push force floor met + repeatability survival rate >= REQUIRED_CLEAN_RATE (0.99) across >= REQUIRED_CLEAN_RUNS (3) episodes, per the 2026-07-04 user decision in exam_verdict.py:26-30 — not clean==runs/100%. Then sweep the other stale "clean==run

## [LOW] REQUIRED_CLEAN_RUNS floor of 3 lets the >=99% show-ready claim rest on as few as 3 held-out episodes
- **Where:** `pipeline/exam_verdict.py:24,110` (safety-gate / statistical-rigor)
- **Why real:** Verified against real code, and the reviewer did NOT miss a guard — there is genuinely no minimum-sample-size check. exam_verdict.py:24 sets REQUIRED_CLEAN_RUNS=3 and line 110 returns `clean >= 3 and (clean/runs) >= 0.99`; a clean=3,runs=3 verdict satisfies both (3>=3, 1.0>=0.99). The line-29 comment confirms the floor of 3 was designed only to stop a literal 1-run 100%, not to support the 99% bar. Reachability is real and not blocked by the HMAC
- **Fix:** Add a MIN_HELDOUT_EPISODES check (e.g. >=100) in derive_pass for the mjlab_heldout path so REQUIRED_CLEAN_RATE=0.99 cannot be satisfied on a handful of samples; the finding's proposed fix is correct.

## [LOW] GPU cost monitor keeps accruing after box deletion — permanent over-cap/spend misreport
- **Where:** `pipeline/monitor.py:90-110` (correctness)
- **Why real:** Verified in real code and reachable; reviewer missed no guard. compute_cost (monitor.py:98) computes hours from created_at to now with no end timestamp; over_cap latches True (line 109). grep across pipeline/ for deleted_at|billing_stopped_at|stopped_at|teardown returns nothing — the only delete in cloud.py:196 is a Jupyter kernel delete, not instance teardown. snapshot (monitor.py:182) computes cost every poll and the docstring (line 179) states
- **Fix:** 

## [LOW] deploy_requests.json uses an unlocked, non-atomic read-modify-write
- **Where:** `ui/server.py:254-257` (robustness/consistency)
- **Why real:** Confirmed against real code. ui/server.py:254-257 performs read_text -> append -> write_text with no lock and no tmp+fsync+os.replace, unlike every sibling store: pipeline/store.py:97-99 (_durable_write+os.replace), pipeline/venue.py:180-182, and pipeline/shows.py:77-83 (_atomic_write) plus _record_lock (shows.py:87,255,312...). deploy_gate is a sync def FastAPI endpoint, so it runs in the threadpool and two concurrent same-job deploy POSTs genui
- **Fix:** Route the write through the same durable helper used elsewhere (write to a .tmp, fsync, os.replace) as in pipeline/store.py:97-99 and pipeline/venue.py:180-182, and wrap it in the pipeline/shows.py _record_lock (per-job dir) so concurrent deploy POSTs can't lose an update or truncate the file. Optionally guard the line-255 json.loads with a try/except so an already-corrupted file self-heals instea

## [LOW] FastAPI engine has no authentication and no CSRF/Origin/Host protection
- **Where:** `ui/server.py:46,283-287,324-329` (security)
- **Why real:** Verified in real code; the reviewer did not miss a guard. server.py creates `app = FastAPI(...)` (line 46) with NO middleware, and a grep across ui/ and pipeline/ for middleware/CORSMiddleware/TrustedHost/Origin/csrf/token/auth confirms there is no CORS middleware, no TrustedHostMiddleware (no Host-header check), and no session/auth token. desktop.py:41 and server.py:581 bind 127.0.0.1. The finding's mechanism analysis is correct: (a) `Body(...)`
- **Fix:** Add a startup-generated per-session token that the pywebview shell injects and that a middleware requires on all /api mutations, and/or add a middleware (or TrustedHostMiddleware plus an Origin check) that rejects any request whose Origin/Host is not the loopback app origin. This closes both the body-less CSRF vectors (/api/cloud/test, /api/bodymodels/install) and the DNS-rebinding vector that oth

## [LOW] Static /previews mount uses follow_symlink=True, removing Starlette's within-directory traversal guard
- **Where:** `ui/server.py:573-574` (security-hardening)
- **Why real:** Code fact is accurate: ui/server.py:573-574 mounts /previews with StaticFiles(follow_symlink=True), which bypasses Starlette's realpath/commonpath within-directory check, so any symlink under data/previews is served regardless of target. Confirmed the only writer of links there is pipeline/stages/local_motion.py:148-150, which links to preview.mp4 inside the job dir — so there is NO attacker-controlled symlink today. The reviewer honestly states 
- **Fix:** Serve previews through an explicit endpoint that resolves the target and asserts Path.resolve().relative_to() an allowlisted job/preview dir before FileResponse, or copy previews into data/previews instead of symlinking so follow_symlink can be turned off. Low priority; combine with binding auth if the server is ever exposed beyond loopback.

## [LOW] Library import has no decompression size/entry-count limit (tar.gz bomb)
- **Where:** `pipeline/library.py:139-146` (robustness)
- **Why real:** Confirmed in real code. _safe_extract (pipeline/library.py:139-146) iterates tar.getmembers() only to reject path-traversal/absolute members, then calls tar.extractall(path) with no cap on total uncompressed size or member count. import_library (line 101-104) invokes it on an operator-supplied archive inside a TemporaryDirectory, and the extracted files are then copied into shows.DANCES_DIR, so a highly-compressible payload can exhaust temp/disk 
- **Fix:** While iterating tar.getmembers() in _safe_extract, sum member.size and count members, aborting past a configured budget (total bytes and member count), and check free disk (mirroring server.py MIN_FREE_DISK_BYTES) before extracting. Extract member-by-member so the abort happens before disk is exhausted rather than via a single extractall.

## [LOW] The in-app video pipeline dead-ends at 'train' — no way to launch cloud training, verify, or turn a job into a dance from the app
- **Where:** `pipeline/stages/local_motion.py:156-174; pipeline/runner.py:40-53; ui/server.py:137-287; ui/static/app.js:243` (workflow-completeness / UX)
- **Why real:** The underlying gap is REAL but the finding is overstated and factually loose on the mechanism.

Confirmed true: TrainStage/VerifyStage/ExportStage.run each just `raise StageBlocked(...)` (local_motion.py:160,167,174); the runner catches StageBlocked and parks the stage as "blocked" (runner.py:50-53). There is NO server route that launches cloud training — server.py exposes /api/jobs {list,get,create,upload,retry,deploy} and /api/cloud {config,tes
- **Fix:** See reasoning; primary fix is actionable blocked-hint + in-app register-dance-from-job using existing routes.
